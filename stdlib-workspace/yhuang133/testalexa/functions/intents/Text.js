const lib = require('lib');

/**
* Basic "Hello World" intent, can receive a `name` parameter
* @param {string} foods ingredients obj
* @returns {any}
*/
module.exports = (link, callback) => {
	//let foodStr = JSON.parse(foods).join(',')
	

};
